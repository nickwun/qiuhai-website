# 秋海个人网站正式上线报告

更新日期：2026-07-30

## 发布范围

- 正式域名：`https://qiuhai.net.cn`
- 网站服务名称：跑步写作手记
- ICP 备案号：闽ICP备2026028446号-1
- 搜索索引：关闭
- 产品购买入口：关闭
- 公安备案：尚未填写，不渲染任何占位信息

## 代码基线

- 发布分支：`codex/launch-qiuhai-website-v1`
- PR：待创建
- 功能 Commit：待创建
- main 合并 Commit：待创建
- `npm run check`：通过，0 错误、0 警告
- `npm test`：通过，27 项测试；响应式检查覆盖 40 个路由/视口组合
- `npm run build`：通过，生成 13 个静态页面
- 服务器初始化脚本：语法检查和 `--dry-run` 通过，未连接服务器
- 手动部署脚本：`--dry-run` 通过，未连接服务器
- GitHub 必需检查：待执行

## 标题与备案显示

- 首页标题：`秋海｜跑步写作手记`
- 内容页标题格式：`页面或文章标题｜跑步写作手记`
- ICP 链接：`https://beian.miit.gov.cn/`
- 公安备案区域：备案号为空，不显示

## 服务器初始化

状态：等待 SSH 主机、管理员用户、实际端口和部署公钥人工确认。

计划使用独立的 `qiuhai-deploy` 用户和 `/var/www/qiuhai`。初始化只安装或检查 Nginx、rsync、curl、UFW，不安装数据库、Docker、宝塔或 Node 常驻服务。

## 首个 release

状态：待服务器初始化和 SSH 公钥验证后执行。

- release ID：待部署
- 当前软链接：待部署
- 上一版本：首个 release 无上一版本；部署脚本会保留后续版本用于回滚
- 健康检查：待执行

## Nginx、DNS 与 HTTPS

- Nginx 配置：待安装和执行 `nginx -t`
- HTTP 服务：待启用
- 服务器公网 IP：待从服务器和控制台交叉确认
- DNS：未修改，等待人工门
- HTTPS：DNS 生效前不申请
- 证书续期 dry-run：待 HTTPS 配置后执行

## 微信公众号发布节点隔离

博客部署只使用 `qiuhai-deploy` 和 `/var/www/qiuhai`，不得读取、修改或重启 `wechat-publisher` 用户、发布程序、环境文件和临时目录。服务器初始化前需先只读确认现有 SSH、UFW、进程和用户状态，避免影响公众号发布流程。

## 上线验收

状态：待 DNS 和 HTTPS 完成后执行。

验收范围包括 HTTPS、跳转、页面标题、ICP 链接、三篇正式文章、图片、404、RSS、Sitemap、robots、sample/draft 排除、索引与购买开关、移动端横向溢出和浏览器控制台。

## 公安备案下一步

网站正式开通后，根据所在地公安机关和全国互联网安全管理服务平台的要求办理公安备案。在取得真实公安备案号前，网站不显示公安备案区域。
