# 秋海个人博客项目规范

## 项目边界

- 本项目只保存准备公开发布的网站源码和内容。
- 不读取、复制或提交私人知识库、原始笔记、凭据、草稿缓存与本地资料导出。
- 迁移公众号或知识库文章时，只接受用户明确指定的公开终稿，并保留原始平台与链接信息。

## 技术约束

- 使用 Astro、TypeScript、Markdown 与 Astro Content Collections。
- 保持纯静态生成，不添加 SSR、数据库、后台、账号、评论、点赞或会员功能。
- 默认不引入 React、Vue、Svelte 等前端框架；样式使用原生 CSS 和 CSS Variables。
- 新增文章必须通过 `src/content.config.ts` 的 Schema，并设置唯一 `slug`。
- `draft: true` 的文章不得进入公开列表、详情路由、RSS 或 sitemap。
- `sample: true` 只用于本地 `/preview/samples/` 排版检查，不得进入生产页面、专题、RSS 或 sitemap；样本不得写成站主真实经历。
- 正式文章必须同时满足 `draft: false` 与 `sample: false`。

## 设计与内容

- 优先保证中文长文阅读：正文宽度约 720px，移动端单栏，无复杂动画。
- 不强制文章封面；没有封面时不渲染空占位。
- 示例或占位内容必须明确标注，不得写成站主真实经历。
- 不补写未经用户提供的经历、动机、心理活动、数字或因果判断。
- 正式文章只能从用户明确提供并允许进入公开仓库的终稿导入；缺少终稿时保留待导入状态，不从记忆、网络或私人知识库补写。
- 正式文章提交前运行 `npm run check:content`，并对家庭成员、学校、班级、路线、住址、机构、工作单位、联系方式和支付信息做人工语境复核。

## Git 与验证

- 写入前检查 `git status --short`，保留并避开用户已有修改。
- 未经当前任务明确授权，不提交、不推送、不发布、不合并 main。
- 提交时精确核对路径，不使用笼统暂存覆盖无关改动。
- 宣称完成前运行 `npm test`、`npm run check` 与 `npm run build`，并检查关键页面的响应式表现。
- 默认只准备腾讯云轻量应用服务器部署资产，不连接服务器、不部署正式域名、不修改 DNS、不申请证书；实际部署需用户在当前任务中另行明确授权。
